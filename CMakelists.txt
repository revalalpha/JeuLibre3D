
# do not touch
cmake_minimum_required(VERSION 3.10.0)

# NameOfTheProject
project(Vulkan_Project VERSION 1.0.0)

# DoNotTouch
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/app/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/app/lib)
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/app/bin)
if (WIN32)
    # vcpkg configuration
    set(VCPKG_ROOT $ENV{VCPKG_ROOT_DIR})
    set(VCPKG_TARGET_TRIPLET "x64-windows")
endif (WIN32)
include(${VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake)
set_property(GLOBAL PROPERTY USE_FOLDERS ON)

# Add you project here
add_subdirectory(Sample)
add_subdirectory(SampleGui)
add_subdirectory(EmptyProject)
add_subdirectory(GLFW_)
add_subdirectory(GameLib)
add_subdirectory(Vulkan)
add_subdirectory(ImGui)
add_subdirectory(Engine)
add_subdirectory(Editor)

# Define the StartUp project
set_property(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT Sample)